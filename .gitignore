*.o
*.so
# These are also compiled artifacts
/src/http.d/*.include

vgcore*
game

data/
!data/.placeholder

# VIM files
*.swp

# Some assets I maybe want to commit so the game runs on a fresh checkout,
# but WIP asset stuff I probably don't want.
*.xcf

# Old stuff I don't want to throw away lol
/assets/old
